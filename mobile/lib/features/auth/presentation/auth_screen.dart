import 'package:flutter/material.dart';

/// Placeholder — replaced by the signup/login/OTP flow in the authentication
/// feature (Milestone 2). Exists now so the navigation shell has a real
/// destination to test against.
class AuthScreen extends StatelessWidget {
  const AuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sign in — coming soon')),
    );
  }
}
